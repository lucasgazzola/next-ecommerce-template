import { ProductInterface } from "interfaces";

function Product({
  id,
  category,
  description,
  image,
  price,
  rating,
  title
}: ProductInterface) {
  return <li></li>;
}

export default Product;
